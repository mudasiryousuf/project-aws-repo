version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo Installing dependencies
      - npm install

  pre_build:
    commands:
      - echo Running tests
      - npm test

  build:
    commands:
      - echo Build successful

  post_build:
    commands:
      - echo Deploying to EC2

artifacts:
  files:
    - '**/*'
